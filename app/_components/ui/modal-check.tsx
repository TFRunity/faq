//Проверяем, уверен ли админ в действии

/**
 *
 * @remarks
 * Модальное окно проверки, уверен ли пользователь в своем действии
 *
 * @param text = Какой текст передаем при рендере компонента
 * @param title = Какой текст передаем в название окна при рендере компонента
 *
 * @returns
 * Компонент
 *
 */